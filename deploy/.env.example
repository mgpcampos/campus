# This value is embedded into the client bundle at build time
# Use your REAL public URL that browsers can reach
# Example for IP-only:
# PUBLIC_POCKETBASE_URL=http://203.0.113.10/pb
# Example for domain + HTTPS:
# PUBLIC_POCKETBASE_URL=https://campus.example.com/pb
