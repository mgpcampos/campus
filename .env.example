# PocketBase Configuration (required)
# Must be the full URL (protocol + host [+ optional port]) where PocketBase is reachable.
# This is the ONLY configuration needed to connect to PocketBase from both frontend and backend.
# Examples:
#   - Local development: http://127.0.0.1:8090
#   - Docker: http://pocketbase:8090
#   - Production: https://pb.yourdomain.com
PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090

# Analytics
PUBLIC_ENABLE_ANALYTICS=false
PUBLIC_ANALYTICS_SAMPLE_RATE=1

# Media Upload Configuration
MEDIA_MAX_IMAGE_SIZE_MB=10
MEDIA_MAX_IMAGE_COUNT=10
MEDIA_MAX_VIDEO_SIZE_MB=500
MEDIA_MAX_VIDEO_DURATION_SECONDS=300